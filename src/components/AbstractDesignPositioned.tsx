import { AbstractDesign } from "./SVGs";

const AbstractDesignPositioned = () => {
  return (
    <div className="relative bottom-[16px] right-5">
      <AbstractDesign />
    </div>
  );
};

export default AbstractDesignPositioned;
